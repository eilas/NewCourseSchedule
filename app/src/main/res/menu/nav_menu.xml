<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/time_date_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/setFirstWeek"
            android:title="@string/setFirstWeek" />
        <item
            android:id="@+id/setItemStrEndTime"
            android:title="设置每格对应时间" />
    </group>
    <group
        android:id="@+id/interaction_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/infoList"
            android:title="信息列表" />
    </group>
    <group
        android:id="@+id/other_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/logout"
            android:title="@string/logout" />
    </group>
</menu>